services:
  ropadesk:
    build: .
    ports:
      - "8081:8080"
    environment:
      ROPADESK_API_KEY: change-me
      ROPADESK_DB: /data/ropadesk.db
    volumes:
      - ropa-data:/data

volumes:
  ropa-data:
